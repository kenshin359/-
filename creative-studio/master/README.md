# master/ — 承認済みマスター（Before基準）

- 完成・承認済みでLOCK対象のクリエイティブを置く。
- 部分修正/差し替えの **Before基準**。差分は必ずここと比較する。
- 命名: `<name>.png`。対応する `../locks/<name>.locks.json` を持つ。
- master は原則不変。修正版は `output/` に出し、承認後にここへ昇格させる。
