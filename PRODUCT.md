# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users
- 株式会社リベティ（Libetee）の経営陣（代表・取締役）と各チーム責任者（CS・広告・LP・SNS・TikTok・O2・社長室・人事管理）。閲覧はスタッフも含む。
- 場面: 毎朝の朝礼前（7:55の朝礼台本と同じ数字を見る）、日中の判断、月次の銀行・計画資料づくり。PCが主、スマホでの確認もある。
- ジョブ: 「今いくら売れ、いくら残り、目標との差と原因、今日やるべきこと」を1画面で判断し、タスクに落とす。
- ロール: admin（全操作）／editor（データ・目標・タスク・資料の編集）／viewer（閲覧のみ。サーバー側でも書込拒否）。

## Product Purpose
EC物販（スーツケース主力・楽天/Amazon/自社サイト）の売上・利益・広告・在庫・タスクを、指標辞書に基づく同一の集計で表示し、改善提案→タスク化→結果確認まで回す経営ダッシュボード。成功＝朝礼・Excel・Kintoneと数字が一致し、経営陣が画面を開いて30秒で判断できること。

## Positioning
数字は必ず現場データ（Kintone実績・取込済みCSV）から計算し、未取得は「未取得」と表示する（成功と偽らない・代用計算しない）。既存の自動運用（Kintone→GitHub Actions→Chatwork）と同じ定義・同じ閾値を使うため、現場の言葉（P1、柳井基準、FBA切れ、必要日販）がそのまま画面に出る。

## Operating Context
- 一次データはKintone（売上明細(29)・毎朝KPI(30)・広告費・在庫報告(35)・タスク管理(38)）。タスクはKintone(38)が正で、ダッシュボードは双方向同期（2026-09-18 北野さん決定）。
- 資料はGoogleドライブに置き、ダッシュボードはリンクとカテゴリを管理する（資料庫）。
- 朝礼台本（毎朝7:55）がタスクボードの期限超過・本日期限・昨日完了を読み上げる。
- 詳細は `dashboard/docs/business.md`（業務知識）・`docs/metrics.md`（指標定義）・`docs/spec.md`（仕様）。

## Capabilities and Constraints
- Next.js 15 App Router + Tailwind + Prisma（開発SQLite／本番Postgres on Vercel）。next-auth credentials。
- タスクの編集権限: editor以上は全員分を編集可、viewerは閲覧のみ（2026-09-18 決定）。
- タスクの必須項目は現場ルールに従う: 期限と「完了の定義（数字）」のないタスクは登録禁止、撤退・判断基準（柳井基準）を先に書く。優先度 P1〜P4、状態 未着手/進行中/確認待ち/完了、売上直結度 ◎/○/△。
- Kintone未接続時は「未接続」を表示し、ローカルDBのタスクで動作する（成功と偽らない）。
- 対象外: ジム予約・人事・採用・給与計算。多企業SaaS化。
- 未決定: 資料庫のカテゴリ一覧（初期値は「全社共有／営業・EC／広告／CS／総務・管理」を仮置き。要確認）。

## Brand Commitments
- 名称「Libetee 経営AIダッシュボード」。既存UI: 濃紺サイドバー（#172554系・220px）＋白カード＋slate系の中立色。これを継承する。
- 表記は日本語。指標名は `docs/metrics.md` の名称を使う。

## Evidence on Hand
- 実データの定義・閾値・組織・運用時刻: `dashboard/docs/business.md`（出典付き）。
- Kintoneタスク管理(38)のフィールド定義: `daily-report-system/kintone/taskBoardSchema.js`、初期タスク例: `scripts/taskBoardSeed.js`。
- 基準となる他社ポータルのスクリーンショット（担当者別カンバン・資料庫）を北野さんが提示（2026-09-18）。この品質を60点とし、上回ることが要件。
- 顧客の声・実績数値の捏造はしない。デモデータは demo=true で分離。

## Product Principles
1. 数字は現場と一致させる。定義・閾値は既存運用と同じものを使い、未取得は未取得と出す。
2. 判断→行動が1画面で完結する。見るだけで終わらせず、タスク化と期限を必ず伴う。
3. 現場の言葉で表示する。P1・柳井基準・FBA切れ・必要日販をそのまま使う。
4. 権限は画面ではなくサーバーで守る。
5. 密度は高く、迷いは少なく。朝礼の15分で読める情報量に絞る。
