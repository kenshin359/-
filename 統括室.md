# リベティ Claude業務 統括室

最終更新: 2026-09-16

この部屋（このファイル）は、**これまでClaudeに依頼して作った業務・仕組み・資料・ルール・判断・未完了事項をすべて1か所にまとめた索引**です。
Claudeは北野さんからの質問に答えるとき、まずこのファイルを読み、必要ならここに書かれたリンク先（docs・スクリプト・設定）を辿って答えます。

> **使い方**: 「統括室」「統括室で聞きたい」「今まで何を作った？」「あの自動化はどこ？」など、
> 過去業務に関する質問はこの部屋で受け付けます。詳しい合言葉は [§2](#2-合言葉と部署) を参照。

---

## 0. 全体マップ（3本柱＋運用ルール）

```
GitHub リポジトリ kenshin359/-（公開リポジトリ）
├── src/                      ① 売上日報ツール（最初期・kintone「売上・転換率報告」アプリの作り直し）
├── daily-report-system/      ② AI日報・売上・広告・在庫・CS 自動化システム（本体。GitHub Actionsで毎日自動実行）
├── dashboard/                ③ 経営AIダッシュボード（Next.js＋Prisma。Vercel＋Neonへ配置予定）
├── .github/workflows/        ②を動かす自動実行スケジュール（50本）
├── CLAUDE.md                 Claude向け運用メモ（合言葉・財務部・ダッシュボード開発ルール）
└── 統括室.md                 ← この部屋
```

| 柱 | 何をするか | 状態 |
|---|---|---|
| ① 売上日報ツール (`src/`) | kintone「売上・転換率報告」(アプリID 7) の文章データをパースして新アプリへ移行・分析する土台 | 完成（2026-07）。以降は②に統合され、`parseReport.js` の解析ロジックが②でも使われる |
| ② AI日報システム (`daily-report-system/`) | kintone→Claude→LINE/Chatwork。日報要約・売上取込・朝礼レポート・広告費・在庫・レビュー返信・経費まで拡張 | **本番稼働中**（GitHub Actionsで毎日自動実行、Chatworkへ通知） |
| ③ 経営AIダッシュボード (`dashboard/`) | 売上・利益・広告・在庫・タスクを1画面で判断するWebアプリ（ログイン制・3ロール） | 初期実装完了（2026-09-16）。Vercel配置はアカウント作成待ち |

---

## 1. 年表（Claudeに依頼して進めた業務の流れ）

Gitの履歴は2026-08-27に1つにまとめられているため、それ以前はドキュメントの日付から復元しています。

| 時期 | 出来事 |
|---|---|
| 2026-07 上旬 | ① 売上日報ツール: 「売上・転換率報告」アプリの自動構築・移行・分析スクリプト（`npm run apps / create-app / migrate / analyze`） |
| 2026-07-28〜31 | ② AI日報システム着手。kintone実構造の調査（日報アプリID 6 は1レコード＝1チーム複数日、権限なし→**kintoneは一切変更せず読み取り側で吸収**する方針を決定）。Claude分析・LINE/Chatwork通知・n8nワークフロー・導入ドキュメント一式 |
| 2026-07 末〜08 上旬 | 売上レポート（CSV→集計はJS側、AIには渡さない方針）、販売ダッシュボードHTML、AI経営チーム（7部署構成）、売上管理シートExcel、楽天レビュー返信下書き（CS Chatwork部屋へ配信） |
| 2026-08 | kintone業務アプリ群を自動作成（日次CSV提出ボックス/売上明細(自動取込)/在庫管理/広告費管理/KPI/経費/販促費/案件/連絡先/議事録/ニュース/タスクボード/ログイン情報 など）。Shopify・Amazon(SP-API)・楽天の売上を毎朝キントーンへ自動取込。朝礼レポート（Chatworkで「朝礼」と打つと返信）、参謀レポート（毎朝8:45）、広告費レポート（毎朝11:02）、在庫CSVダンプ、CSレビュー受信箱、経費レポート、SNS動画分析ハンドブック |
| 2026-08-17 | 8月の朝礼用イベントカレンダー・手動広告費（ガジェティ/TDA/TikTok/案件費）を設定ファイル化 |
| 2026-08-27 | 在庫画像取り出しワークフロー追加。以降、売上通知記録・在庫CSVダンプ・広告費デバッグは**自動コミット**で毎日更新 |
| 2026-09-01 | 広告費レポートに対象月指定（`--month`）を追加（8月固定判定を解消） |
| 2026-09-06 | 月次SKUまとめに商品カテゴリ別・媒体別の月間個数/売上ダンプを追加。SKU対応表を更新 |
| 2026-09-09〜10 | 広告費の商品別ダンプ（媒体×商品カテゴリ）。メタCSV英語表記対応、ファイル名日付が未来でも中身で取込、調査モード（`--files`, peek） |
| 2026-09-11 | 在庫ダンプに注文・顧客データらしきCSVを出力しないガードを追加（公開リポ対策） |
| 2026-09-15 | 合言葉「大怪獣ワチソンSKU」登録。財務・資金調達部（AI3人体制）をCLAUDE.mdに登録。商品別広告費でジップタイプを分類 |
| 2026-09-16 | ③ 経営AIダッシュボード初期実装（認証・指標辞書・経営サマリー・ユーザー管理）。VPS/Docker一式→配置先を**Vercel＋Neon**に決定。統括室（この部屋）を新設 |

---

## 2. 合言葉と部署

北野さんが送る「合言葉」と、Claudeが取る行動。**正式な手順は `CLAUDE.md` が正**（ここは索引）。

| 合言葉 | 成果物 | データの取り方（要点） |
|---|---|---|
| **大怪獣ワチソン** | 当月「売上vs目標 進捗Excel」（サマリー・日別実績(媒体別)・目標乖離） | 「月次SKU別まとめ」ワークフローを当月でdispatch→ログのA-J復号で日別×媒体売上。目標は `config/chorei/events-YYYY-MM.json`（イベント日加重、メイン1.1億円） |
| **大怪獣ワチソンCPA** | 当月「合算CPA Excel」（スーツケース系の日別×媒体 個数・広告費・CPA・売上・広告比率） | 月次SKU別まとめ（`===DAILY_CH_UNITS_B===` / `===DAILY_SC_SALES_B===`）＋広告費レポート dump（`===ADCOST_DAILY_B===`、正規表現は `:\s*([A-J]+)`）→ `cpa_inputs.json` → `scripts/buildCpaSheet.py`。判定: 目標CPA¥4,500🟢/許容¥6,000🟡/超過🔴、広告比率15%/20% |
| **大怪獣ワチソンSKU** | 当月「SKU別販売まとめExcel」（SKU×媒体、商品グループ見出し、サマリー） | 月次SKU別まとめの `===SKU_B===` をA-J復号。表示名は `config/sku-names.json` |
| **財務部 / 財務部で進めて** | 銀行提出資料（事業計画書Excel・試算表チェック・銀行別借入一覧Excel） | 経営企画AI／経理AI／財務AIの3人体制。**数字は現場データか北野さん提供資料のみ、不明値は黄色セル＋「要データ」** |
| **朝礼**（Chatwork売上グループで発言） | 📣 朝礼レポート（昨日までの売上と計画比） | `scripts/chorei.js` が朝の時間帯にChatworkを見て自動返信（GitHub Actions） |
| **統括室** | この部屋を読んで、過去業務の所在・状態・手順を答える | このファイル→リンク先docs |

### 共通ルール（合言葉全体）
- **古いデータの使い回し禁止**。必ず実行時にワークフローをdispatchして最新ログから取る。前日分が未取込なら Amazon売上取込＋売上検算を先にdispatch。
- ログの数字は **0-9→A-J に置換**されている（GitHub Actionsのシークレットマスク対策）。読むときは復号する。
- Excelは生成→recalc（数式再計算）→送付。

---

## 3. 毎日の自動実行スケジュール（GitHub Actions・日本時間）

ワークフローは `.github/workflows/` に全51本。ファイル名の時刻（0800等）と実際の実行時刻はGitHub Actionsの混雑回避のためずらしてある。**手動実行は GitHub → Actions → 該当ワークフロー → Run workflow**（Claudeは「dispatch」と呼ぶ）。

### 3.1 定期実行（1日の流れ）

| 時刻(JST) | ワークフロー | やること | 届く先 |
|---|---|---|---|
| 6:56、7:06〜10:56 の10分おき | `chorei.yml` 朝礼レポート | Chatworkに「朝礼」と発言があれば売上・計画比を返信 | Chatwork 売上ルーム |
| 7:12 | `shopify-sales-0800.yml` | Shopify（自社サイト）売上を直近3日ぶん再取込 | kintone 売上明細（自動取込） |
| 7:19 | `amazon-sales-0815.yml` | Amazon SP-APIから売上取込 | 同上＋Chatwork売上ルーム |
| 7:26 | `rakuten-sales-0830.yml` | 楽天RMSから確定売上を直近3日ぶん再取込 | kintone 売上明細（自動取込） |
| 7:33 | `cs-reviews-0800.yml` | 楽天の未返信レビュー（7日）→返信文付きExcel | Chatwork CSレビューコピペルーム |
| 7:52 | `daily-brief-0845.yml` 参謀レポート | 3チャネル合算の1日まとめ（目標達成率・媒体別・商品別・前月比・計画比） | Chatwork 売上ルーム |
| 7:55 | `chorei-script.yml` 朝礼台本 | 15分朝礼台本＋進捗シートExcel（売上・件数・広告費・必要日販） | Chatwork 朝礼ルーム |
| 11:02 | `ad-cost-1100.yml` 広告費レポート | KPIアプリ添付CSVから前日までの媒体別広告費 | Chatwork 朝礼ルーム |
| 11:10 | `news-daily.yml` デイリーニュース | 昨日の売上・個数・転換率・広告費を記事化 | kintone リベティ・デイリーニュース |
| 11:20 | `sales-gap-check.yml` | 手入力「売上・転換率報告」と自動「売上明細」を突合、±1万円超の差異日を通知 | Chatwork 朝礼ルーム（差異時のみ） |
| 11:30 | `ad-csv-gap-check.yml` | KPI報告の広告CSV未添付を検出して催促 | Chatwork 朝礼ルーム（抜け時のみ） |
| 12:41 / 18:41 | `sales-recheck.yml` 売上検算 | kintoneと楽天/Shopify APIの最新値を突合、ずれた日だけ再取込 | kintone（修正）＋Chatwork（修正時のみ） |
| 19:07 | `sales-report-1900.yml` | 売上・転換率報告に新規記載日があれば分析＋直近14日Excel | Chatwork 売上ルーム。`state/notified-sales.json` を自動コミット |
| 21:17 | `rakuten-sales-0830.yml`（夜モード） | 楽天の当日速報取込 | kintone 売上明細 |
| 毎月22日 7:41 | `expense-report-monthly.yml` | 経費（前月22日〜当月21日）を利用者別・費目別PDF | Chatwork 経理ルーム＋Artifact(90日) |

### 3.2 手動実行（資料づくり・データ取り出し）— ログに `===XXX_B===` マーカーで出力（数字はA-J符号化）

| ワークフロー | 入力 | 取れるもの（マーカー） | 主な用途 |
|---|---|---|---|
| `monthly-sku-daily.yml` 月次SKU別まとめ | `month`(空=先月) | `===DAILY_B===` `===SKU_B===` `===CAT_MONTH_B===` `===DAILY_CH_B===` `===DAILY_CH_UNITS_B===` `===DAILY_SC_SALES_B===` `===CAT_MONTH_CH_B===` | **大怪獣ワチソン全種の元データ**、在庫消化カレンダー、財務部の売上計画 |
| `monthly-units.yml` 月次個数まとめ | `month` | `===UNITS_JSON===`（媒体×商品の個数・売上） | 資料作成 |
| `ad-cost-1100.yml`（`dump: true`） | `month`, `files`, `peek` | `===ADCOST_DAILY_B===`（日別×媒体広告費）`===FILES===` `===PEEK===` | CPA Excel、広告費調査 |
| `ad-product-dump.yml` | `month` | `===ADPROD_B===`（媒体×商品カテゴリの月間広告費） | 商品別広告費・ROAS資料 |
| `kpi-dump.yml` | `days`, `app` | `===KPI_META_HEX===`（KPI添付を16進符号化） | 広告費・売上レポートの原本調査 |
| `cvr-dump.yml` | `days` | `===CVR_B===`（アクセス数・転換率） | 転換率分析 |
| `stock-csv-dump.yml` / `stock-report-dump.yml` / `stock-image-dump.yml` | `days` / `index` | `===CSV_B===` `===STOCK_B===` `===INV_B===` `===IMG_B===` | 在庫Excel、事務所在庫スクショ取得（`state/stock-dump.txt` 自動コミット） |
| `adcost-debug.yml` | push（`state/adcost-debug-request.json` を書き換え） | `state/adcost-debug.txt` に自動コミット | Meta広告費の内訳検証 |
| `reply-pack.yml` / `review-inbox.yml` | `days`, `room` | Excel / PDF をChatworkへ | レビュー返信パックの臨時送付 |
| `app-list.yml` / `app-inspect.yml` / `space-menu.yml` | `apps` 等 | kintoneアプリ一覧・構成・スペースリンク集 | kintone棚卸し |
| `calendar-events.yml` / `task-import.yml` / `consolidate-apps.yml` / `rename-apps.yml` / `kintone-sync-options.yml` | 各種 | kintoneへの一括登録・統合・改名・選択肢同期 | kintone整備 |
| `shopify-token.yml` / `spapi-check.yml` | `code` | Shopifyトークン発行（Chatworkシークレットルームへ）/ SP-API鍵の形式チェック | 初回設定・トラブル時 |
| `create-*-app.yml`（11本）/ `update-*-app.yml`（5本） | 一部 `seed`(base64) | kintoneアプリの新規作成・項目更新 | アプリ構築（§5.1参照） |

### 3.3 自動コミットされるファイル（毎日の「（自動）」コミットの正体）

| コミットメッセージ | ファイル | 元ワークフロー |
|---|---|---|
| 売上通知の記録を更新（自動） | `daily-report-system/state/notified-sales.json` | `sales-report-1900.yml` |
| 在庫CSVダンプを更新（自動） | `daily-report-system/state/stock-dump.txt` | `stock-image-dump.yml` |
| 広告費デバッグ結果を更新（自動） | `daily-report-system/state/adcost-debug.txt` | `adcost-debug.yml` |

---

## 4. 業務カタログ（領域別）

`daily-report-system/scripts/`（86本）を業務単位でまとめたもの。`npm run <名前>` は `daily-report-system/` で実行。空欄はnpmエイリアス無し（GitHub Actionsから直接呼ばれる）。
**注意**: `daily-report-system/README.md` の構成図・コマンド表は初期版のままで半分以上が載っていない。**この表を正**とする。

### 4.1 売上（取込・検算・レポート）

| 業務 | スクリプト | npm | 中身 |
|---|---|---|---|
| Amazon売上の自動取込 | `amazonImport.js` | `amazon:import` | SP-API→売上明細(29)へ日単位upsert（Amazon分だけ入替）。`--date` `--from/--to` `--dry-run` |
| 楽天売上の自動取込 | `rakutenImport.js` | `rakuten:import` | RMS楽天ペイ受注API→商品別集計→upsert。朝=確定3日分、夜=当日速報 |
| Shopify売上の自動取込 | `shopifyImport.js` | `shopify:import` | Admin API→upsert（他媒体は残す） |
| CSVからの売上取込 | `salesImport.js` | `sales:import` | 提出ボックス/手元CSV→商品紐づけ（`sku-map.json`）→売上明細。`--dry-run`で未登録SKU一覧 |
| 売上検算（自動修正） | `salesRecheck.js` | — | kintoneと楽天/Shopify APIの最新値を突合、ずれた日だけ再取込しChatworkへ差分 |
| 手入力と自動集計の差額監視 | `salesGapCheck.js` | — | アプリ7と29を媒体別に突合、±1万円超の日だけ通知 |
| 売上速報（CSVフォルダから） | `salesDigest.js` | `sales` | `data/sales/` の5媒体CSVを1通に（初期の仕組み） |
| 月次売上レポート | `monthlySalesReport.js` | `monthly` | アプリ7から総売上/前日比/前月比 |
| 月次個数・SKU取り出し | `monthlyUnits.js` / `monthlySkuDaily.js` | `monthly:units` / `monthly:sku` | 資料用ダンプ（A-J符号化）。大怪獣ワチソンの元データ |
| 新規記載日の売上通知＋簡易Excel | `watchSales.js` + `buildQuickSheet.py` | `watch:sales` / `quicksheet` | 毎晩19:07。直近14日の簡易シート添付 |
| 売上管理シートExcel | `exportDashboardData.js` → `buildSalesSheet.py` → `sendSalesSheet.js` | `sheet` / `sheet:send` | 日次/商品別/商品×日/月次の4シート（数式付き） |
| 販売ダッシュボードHTML | `exportDashboardData.js` → `buildDashboard.js` | `dashboard` | `out/dashboard.html`（テンプレ `dashboard/template.html`）※③のWebダッシュボードとは別物 |
| 合算CPA Excel | `buildCpaSheet.py` | — | `cpa_inputs.json` → `合算CPA_<month>.xlsx`（合言葉CPA用） |
| 転換率・KPI添付の取り出し | `cvrDump.js` / `kpiDump.js` | — | アクセス数・転換率 / KPIアプリ(30)添付の原本 |
| n8n売上ワークフロー再生成 | `buildN8nSales.js` | `build:n8n` | `snippets/salesInline.js` を workflow-6 に埋め込み |

### 4.2 広告費

| 業務 | スクリプト | npm | 中身 |
|---|---|---|---|
| 広告費デイリー報告（毎朝11:02） | `adCostReport.py`（共通 `adlib.py`） | — | KPI(30)添付CSVから当月の媒体別広告費。手動分は `chorei/adcost-extra-*.json`。`--dump` `--files` `--peek` `--month` |
| 広告CSVの取込（広告費管理アプリへ） | `importAdCosts.js` / `adReport.js` | `ads:import` / `ads:report` | Meta/Amazon/RPP/Google/TikTokを媒体単位で入替。昨日＋今月の商品別・媒体別 |
| 商品別広告費ダンプ | `adProductDump.py` | — | 媒体×商品カテゴリの月間広告費（RPP=商品管理番号/Amazon=キャンペーン名/Meta=広告セット名で分類、ジップタイプ対応済み） |
| Meta広告費の内訳検証 | `adCostDebug.py` | — | `state/adcost-debug-request.json` を書き換えてpush→結果が `state/adcost-debug.txt` に自動コミット |
| 広告CSV未添付の催促 | `adCsvGapCheck.js` | — | ブランド別添付欄（リベティ4/O2 2/ガジェティ1）の抜けを通知 |
| KPIアプリの広告欄追加 | `updateKpiAdFields.js` | — | ブランド×媒体の添付欄を追加（冪等） |

### 4.3 在庫

| 業務 | スクリプト | npm | 中身 |
|---|---|---|---|
| 欠品・要補充チェック | `stockCheck.js` | `stock:check` | 在庫管理(28)から欠品/残りわずか/要補充。`--low=` |
| 在庫報告・在庫CSV・在庫画像の取り出し | `stockReportDump.js` / `stockCsvDump.js` / `stockImageDump.py` | — | 在庫報告(35)の残在庫・添付FBA在庫CSV・事務所在庫スクショ（base64） |

### 4.4 朝礼・日報・ブリーフ・ニュース

| 業務 | スクリプト | npm | 中身 |
|---|---|---|---|
| 朝礼レポート（Chatwork「朝礼」に自動返信） | `chorei.js` | — | 売上ルームを巡回。目印📣で二重返信防止 |
| 朝礼台本＋売上進捗シート（毎朝7:55） | `choreiDaily.js` / `choreiSheetData.js` → `buildChoreiSheet.py` → `sendChoreiSheet.js` | — | 司会（西岡さん）へ15分台本。必要日販を1.1億・1.2億の両建て。イベント加重シートを朝礼ルーム（みさきさん）へ |
| 参謀レポート（毎朝7:52） | `dailyBrief.js` | `daily:brief` | 3チャネル合算、目標達成率、`daily-plan-YYYY-MM.json` との計画比 |
| デイリーニュース（毎朝11:10） | `newsCvr.js` → `newsDaily.py` | — | 売上・個数・転換率・広告費・良い/悪い広告を記事化しニュースアプリ(39)へ |
| AI経営チームのブリーフィング | `boardBrief.js`（`lib/aiTeam.js`、`prompts/team/`） | `brief` | 7部署が並行分析→統括が1枚に |
| 日報の日次まとめ / 提出即時通知 | `dailyDigest.js` / `watchNewReports.js` | `digest` / `watch` | 日報アプリ(6)全員分＋未提出者 / 新規提出をClaude要約してLINE |
| 日報パイプライン（初期の中核） | `fetchDailyReports.js` → `callClaude.js` → `saveAiReport.js` → `sendLine.js`、通し `runPipeline.js`、緊急 `urgentNotify.js` | `fetch` `analyze` `save` `line` `pipeline` `urgent` | kintone日報→Claude→AI経営日報アプリ→LINE |
| 夜間定時処理 | `nightly.sh` | `nightly` | 売上通知＋通知済み記録コミット |

### 4.5 レビュー・CS返信（楽天）

| 業務 | スクリプト | npm | 中身 |
|---|---|---|---|
| 返信パックExcel（毎朝7:33） | `replyPack.js` → `buildReplyPackXlsx.py` → `sendReplyPack.js` | `replies:pack` / `replies:pack:send` | 過去7日の未返信レビュー全件に返信文（AIまたはテンプレ `reply-blocks.json`・投稿済み323件準拠）。★3以下・安全/不良/返金は「要確認」 |
| 返信下書きを1件ずつ配信 | `replyDrafts.js` | `replies` | CSレビューコピペルームへ（投稿は人が行う） |
| 返信待ちレビューPDF | `reviewInbox.js` → `buildReviewInboxPdf.py` → `sendReviewInbox.js` | `reviews:inbox` / `reviews:inbox:send` | 店舗未返信だけ抽出、低評価が先頭 |
| レビュー分析レポート / 商品改良提案書 | `analyzeReviews.js` → `buildReviewReport.py` / `analyzeProductIssues.js` → `buildProductReport.py` | `reviews:report` / `product:report` | 評価軸の数値化PDF / 部位別の不満を降ろした改良提案PDF（AI不使用） |
| レビュー用商品IDの解決 | `resolveRakutenItems.js` | `rakuten:items` | 商品ページURL→`config/rakuten-items.json` |

### 4.6 経費・SNS・その他資料

| 業務 | スクリプト | npm | 中身 |
|---|---|---|---|
| 経費レポートPDF（毎月22日） | `expenseReport.js` → `buildExpensePdf.py` → `sendExpensePdf.js` | `expense:report` / `expense:send` | 経費管理(49)の前月22日〜当月21日を利用者別/費目別/支払方法別に。経理ルームへ |
| SNS動画分析・運用ハンドブック | `analyzeVideos.py` → `buildSnsHandbook.py` | `videos` / `handbook` / `sns` | `data/videos/` のmp4を数値化（尺・カット数・テロップ位置等）→ノウハウPDF（担当者退職への備え） |
| PDF共通部品 | `pdf_common.py` | — | IPAゴシック前提 |

### 4.7 kintone整備・セットアップ・ユーティリティ

| 業務 | スクリプト | npm | 中身 |
|---|---|---|---|
| 初期セットアップ / 総点検 / 通知テスト | `setup.js` / `doctor.js` / `sendTest.js` | `setup` / `doctor` / `notify-test` | 対話式で `.env` 作成、4サービス実接続チェック |
| バックアップ | `backup.js` | `backup` | 全レコードを `backups/` へ（読み取りのみ） |
| 提出ボックスの確認・取込 | `intakeCheck.js` / `intakeImport.js` | `intake:check` / `intake:import` | 8枠の不足通知（そろっていても送る）/ CSV→広告費管理・売上集計 |
| Shopifyトークン発行 | `shopifyToken.js` | `shopify:token` | OAuth。トークンはChatwork経由で受取（ログに出さない） |
| LINE宛先ID確認 | `lineWebhookPeek.js` | — | 設定時のみ |
| アプリ棚卸し・改名・統合・スペース | `listAppsUsage.js` / `renameApps.js` / `consolidateApps.js` / `spaceMenu.js` | — | 一覧 / 【🤖自動入力】【✍手動】の目印付与 / 会社名簿(40)・会社カレンダー(44)への統合 / リンク集 |
| 初期データ投入 | `contactsSeed.js` / `loginInfoSeed.js` / `minutesSeed.js` / `taskBoardSeed.js` / `taskImport.js` / `calendarEventsSeed.js` | — | base64(`SEED_RECORDS_B64`/`TASKS_B64`)で受け取り、中身はログに出さない。カレンダーは楽天・Amazon公式日程 |
| アプリ作成・項目更新（`kintone/`） | `createApps.js` / `createBusinessApps.js <kind>` / `inspectApp.js` / `addFields.js` / `syncSalesOptions.js` / `update*.js` | `create-apps` / `create-business-apps` / `inspect` / `add-fields` / `kintone:sync-options` | スキーマは `kintone/*Schema.js`、一覧・グラフは `*Views.js`、ニュースのカード型ビューは `newsCustomize.js`＋`customize/newsFeed.js` |

### 4.8 n8n ワークフロー（`daily-report-system/n8n/`・ブラウザだけで動かす経路）

| # | ファイル | 内容 |
|---|---|---|
| 0 | `workflow-0-setup.json` | AI経営日報アプリの自動作成（1回だけ） |
| 1 | `workflow-1-daily-report.json` | 定時19:00: 日報取得→Claude→保存→LINE |
| 2 | `workflow-2-urgent-webhook.json` | 緊急Webhook→シークレット検証→Claude要約→LINE即時 |
| 3 | `workflow-3-notify-on-submit.json` | 日報提出の即時通知（10分ごと、添付画像も読む） |
| 4 | `workflow-4-line-id-finder.json` | LINE送信先IDの調査（設定時のみ） |
| 5 | `workflow-5-daily-digest.json` | 日次まとめ（全員分＋未提出者） |
| 6 | `workflow-6-sales-report.json` | 売上レポート10:00（**自動生成物**、`npm run build:n8n`） |
| 7 | `workflow-7-shopify-sales.json` | Shopify売上→kintone（毎朝8:00） |

※ 現在の定時実行の実体は **GitHub Actions（§3）**。n8nは導入初期の経路として残している。

### 4.9 経営AIダッシュボード（`dashboard/`・③）

| 項目 | 内容 |
|---|---|
| 技術 | Next.js 15.1 + Prisma（開発SQLite / 本番Postgres）+ next-auth（credentials・JWT・admin/editor/viewer）+ Recharts + Vitest |
| 実装済み | ログイン・ロール制御（サーバー側でも403）、経営サマリー画面（KPI5・モール別・日別推移・目標vs着地・ウォーターフォール・TOP10・パイプライン・タスク・提案・アラート）、指標辞書と検算17件、デモデータ分離、ユーザー管理画面（管理者専用）、初回管理者の自動作成、ログイン10回失敗で15分ロック（DB保存） |
| 初期ユーザー | 代表 塚本崚太郎 / 取締役 北野拳慎（管理者。塚本さんは仮ID） |
| 配置 | **Vercel＋Neon**（`vercel.json` がマイグレーション→初回管理者作成→ビルド。Root Directory=`dashboard`、Production Branch はダッシュボードのコードがあるブランチ）。代替: VPS＋Docker（`Dockerfile` / `docker-compose.yml` / `Caddyfile`） |
| コマンド | `npm run dev` / `npm run test`（検算）/ `npm run build` / `npm run seed` / `scripts/create-user.ts` |
| 正となる文書 | `docs/spec.md`（仕様）`metrics.md`（指標）`data-contracts.md`（CSV/DB）`progress.md`（進捗）`ops.md`（配置） |

### 4.10 売上日報ツール（`src/`・①・最初期）

`npm run apps`（アプリ一覧）/ `create-app`（「売上日報（新）」作成）/ `migrate`（アプリ7の文章をパースして投入、`DRY_RUN=1`でプレビュー）/ `analyze`（月次サマリー）。解析ロジック `parseReport.js` は②の `lib/kintoneSales.js` 系でも同形式を扱う。

---

## 5. データの所在

### 5.1 kintoneアプリ（サブドメイン `w6pq7i12hn4b.cybozu.com`）

アプリ名の目印: 【🤖自動入力】＝機械が書く、【✍手動】＝人が入れる（`renameApps.js` が付与）。IDはワークフロー既定値・スクリプトから判明したもの。

**元からあるアプリ（本システムは読むだけ）**

| ID | アプリ | 中身 | 扱い |
|---:|---|---|---|
| 6 | 日報 | 1レコード＝1チーム複数日、本文は自由記述 | 読み取りのみ（`lib/extractReports.js`） |
| 7 | 売上・転換率報告（手入力） | 1レコード＝1ヶ月、【売り上げ】【ランキング】【詳細数値】形式 | 読み取りのみ。`KINTONE_SALES_APP_ID` |
| 8 / 34 / 16 / 17 | 顧客リスト / 社員名簿 / Event Calendar（会社・個人） | 旧アプリ | 40・44へ統合済み（`consolidateApps.js`） |

**Claudeが作って運用しているアプリ**

| ID | アプリ | 中身 | 環境変数 / 作成元 |
|---:|---|---|---|
| 28 | 在庫管理 | Amazon(FBA)在庫とCS在庫（倉庫＋事務所）を分けた1日1レコード＋元ファイル添付 | `KINTONE_INVENTORY_APP_ID` / `createBusinessApps inventory` |
| 29 | 売上明細（自動取込）【🤖】 | 1レコード＝1日、明細に販売先×商品の数量・売上、日次CSV自動添付。**売上系の単一の真実**（朝礼・参謀・ニュース・月次SKUまとめの元） | `KINTONE_SALES_DETAIL_APP_ID` / `createBusinessApps salesdetail` |
| 30 | 毎朝KPI報告・広告CSV添付【✍毎日】 | ブランド×媒体の広告CSV添付欄、媒体別売上/広告費→達成率・広告費率の自動計算、PDCA欄。**広告費レポート・CPAの元** | `KINTONE_KPI_APP_ID` / `createBusinessApps kpi` |
| 32 | 案件報告 クリエイター管理【✍随時】 | Meta広告クリエイター配信管理（配信可否報告期限＝投稿日＋3日） | `createBusinessApps anken` |
| 35 | 在庫報告 CS出荷後【✍毎日】 | CSが出荷後に入力する商品別残在庫＋FBA在庫CSV・事務所スクショ添付 | `KINTONE_STOCK_REPORT_APP_ID` / `createBusinessApps stockreport` |
| 36 | 案件依頼 インフルエンサーPR【✍随時】 | ギフティング・PRタイアップ・テレビ出演・プレスリリースの受付〜投稿完了 | `KINTONE_ANKEN_IRAI_APP_ID` / `createBusinessApps ankenirai` |
| 38 | タスク管理 チーム進捗【✍毎朝】 | 柳井ルール（期限と完了の定義（数字）が無いタスクは登録禁止） | `KINTONE_TASK_APP_ID` / `createBusinessApps taskboard` |
| 39 | リベティ・デイリーニュース【🤖】 | 1日1記事、カード型ビュー | `KINTONE_NEWS_APP_ID` / `createBusinessApps news` |
| 40 | 会社名簿 社員・顧客・専門家【✍随時】 | 弁護士・社労士・税理士・銀行などの連絡先 | `createBusinessApps contacts` |
| 41 | ログイン情報 社内アカウント【✍随時】 | サービス名・URL・用途（パスワード本体は置かない） | `createBusinessApps logininfo` |
| 42 | 販促費管理（広告費記載／全て）【✍毎日】 | Google/TikTok広告・案件依頼費・TV出演費などCSVに乗らない支出 | `KINTONE_PROMO_APP_ID` / `createBusinessApps promocost` |
| 43 | 議事録 ミーティング記録【🤖】 | 要約・決定事項・未決事項・録音リンク | `createBusinessApps minutes` |
| 44 | 会社カレンダー 統合【✍随時】 | 社内予定＋楽天/Amazon公式イベント日程 | `KINTONE_CALENDAR_APP_ID` / `consolidateApps --mode=calendar` |
| 49 | 経費管理（法人カード・立替） | 1件1レコード、締めは前月22日〜当月21日 | `KINTONE_EXPENSE_APP_ID` / `createBusinessApps expense` |
| — | 日次CSV提出ボックス | 1日1レコード、売上4枠＋広告4枠＋取込ログ | `KINTONE_INTAKE_APP_ID` / `createBusinessApps intake` |
| — | 広告費管理 | 1日1レコード、媒体×商品×広告費 | `KINTONE_ADCOST_APP_ID` / `createBusinessApps adcost` |
| — | AI経営日報 | Claude生成サマリー（17項目、`target_date` unique） | `KINTONE_AI_REPORT_APP_ID` / `createApps.js` |
| — | スタッフ日報（新規用スキーマ） | 実運用では未使用（既存アプリ6を読む方針のため） | `staffReportSchema.js` |

### 5.2 設定ファイル（`daily-report-system/config/`）— コミットして良いのは集計値まで

| ファイル | 中身 | 更新タイミング |
|---|---|---|
| `chorei/events-YYYY-MM.json` | 月間目標（main 1.1億 / stretch 1.2億）とイベント日・重み（楽天SS 2.0 / マラソン 1.7 / 最終日 1.2〜1.3） | 月初。9月は楽天SS 9/4〜11、マラソン 9/19〜24 登録済み。**9月目標は8月と同額を仮置き** |
| `chorei/amazon-manual-YYYY-MM.json` | Amazon日別売上の手動控え（SP-API停止中の補完） | 添付漏れの日だけ |
| `chorei/adcost-extra-YYYY-MM.json` | 自動で取れない広告費（ガジェティ/TDA/TikTok/案件費/TV） | 値が変わったとき（8月分のみ存在） |
| `daily-plan-2026-08.json` | 8月の日別売上計画（1.2億・楽天68M/Amazon27M/自社25M日割り）。参謀レポートの「計画比」に使用 | 月ごとに新ファイル（**9月分は未作成**） |
| `sku-names.json` | SKUコード→表示名（大怪獣ワチソンSKU用） | SKU追加時 |
| `sku-map.json` | 売上CSVの商品→SKU/商品名の紐づけ（Amazon/Shopify/楽天共通） | 商品追加時 |
| `ad-campaign-rules.json` | 広告キャンペーン名→媒体・商品の判定表（確定/要確認） | 新キャンペーン時 |
| `cost-map.json` | 商品別コスト（送料・手数料込み、広告費除く）。**スーツケースS/M/Lのみ登録、他は原価待ち** | 原価判明時 |
| `product-aliases.json` | 資料ごとの商品名ゆれの統合表 | 名称追加時 |
| `rakuten-items.json` / `sales-mapping.json` / `reply-blocks.json` | 楽天商品ID対応 / 売上列マッピング / レビュー返信の定型ブロック | 随時 |

### 5.3 状態ファイル（`daily-report-system/state/`・自動コミット）

| ファイル | 中身 |
|---|---|
| `notified-sales.json` | 売上通知済みの日付一覧（二重通知防止・毎日自動更新） |
| `replied-reviews.json` | 返信下書き作成済みレビューID |
| `stock-dump.txt` | 在庫CSVダンプ（FBA在庫CSVをA-J符号化。事務所在庫スクショはskip） |
| `adcost-debug-request.json` / `adcost-debug.txt` | 広告費デバッグの要求（日・nonce）と結果 |

### 5.4 Secrets（GitHub Actions・名前のみ）
kintone: `KINTONE_BASE_URL / KINTONE_USER / KINTONE_PASSWORD / KINTONE_*_APP_ID`。
Chatwork: `CHATWORK_API_TOKEN` と部屋ID `CHATWORK_ROOM_ID`(テスト用) / `CHATWORK_SALES_ROOM_ID`(売上) / `CHATWORK_CHOREI_ROOM_ID`(朝礼) / `CHATWORK_CS_ROOM_ID`(CSレビューコピペ) / `CHATWORK_SECRET_ROOM_ID`。
外部連携: `SHOPIFY_*`(OAuthで発行済みトークン) / `SPAPI_*`(Amazon) / `RAKUTEN_*` / `ANTHROPIC_API_KEY` / `SALES_TARGET_MONTHLY`。
**値は絶対にリポジトリに書かない**（`.env` と `*.db` はgitignore）。

---

## 6. 決めたこと（設計判断・運用ルール）

| 判断 | 理由 | 出典 |
|---|---|---|
| kintoneは**読むだけ**。既存アプリの構造は変えず、読み取り側で吸収 | 日報アプリの管理権限が無い／スタッフの入力方法を変えない | `docs/actual-kintone-structure.md`、`test/readonly.test.js` で自動保証 |
| 自動取込の売上は既存の手入力アプリに書き足さず、**別アプリ「売上明細（自動取込）」** | どちらが正しいか分からなくなるのを防ぎ、突き合わせで差に気づける | `docs/kintone-sales-detail-app.md` |
| **金額の集計はJS側**、AIには結果だけ渡す（通常運用のAPI費用ゼロ） | LLMに加算させると金額がずれる | `README.md` 売上レポート |
| Amazon在庫とCS在庫を**分けて**記録 | FBA在庫は楽天・自社注文に使えない | `docs/kintone-inventory-app.md` |
| 広告のキャンペーン→商品は**確実なものだけ確定**、不明は「要確認」 | 誤って別商品に付けると赤字商品を黒字と誤認 | `config/ad-campaign-rules.json` |
| ログの数字は **A-J符号化** | GitHub Actionsのシークレットマスクで数値が***になる | `scripts/monthlySkuDaily.js` |
| 公開リポジトリに置くのは**日別・媒体別の集計値まで**。明細・顧客・認証は置かない | 公開リポジトリ | `config/chorei/README.md`、在庫ダンプのガード（2026-09-11） |
| レビュー返信は**下書きまで**、投稿は人が行う | 楽天にAPIが無い＋人が一度も見ない文面を出さない | `docs/review-reply-setup.md` |
| AI経営チームは**7部署構成**、データが無い部署は動かさない | 視点の平均化を防ぐ／幻の指摘と費用を防ぐ | `prompts/team/README.md` |
| ダッシュボード: 集計は `src/lib/metrics/` のみ、demo=true は常に排他、未接続は「未接続」表示、ROASは帰属売上未取得なら「未取得」 | 画面ごとの独自計算・偽の成功表示を禁止 | `dashboard/docs/metrics.md`、`CLAUDE.md` |
| ダッシュボードDBは開発SQLite→本番Postgres(Neon)。配置は**Vercel＋Neon**（VPS/Dockerは代替） | 共用レンタルサーバーはNode常駐不可。Vercel Hobbyは商用不可→**Pro必須** | `dashboard/docs/ops.md`、`progress.md` |
| 財務部の数字は現場データか北野さん提供資料のみ。不明は黄色セル＋「要データ」 | 銀行提出資料に推測値を入れない | `CLAUDE.md` |
| 目標はイベント日加重（通常1.0/マラソン1.7/SS・スマイルセール2.0/最終日1.2〜1.3） | セール日の売上偏りを日次目標に反映 | `CLAUDE.md`、`config/chorei/events-*.json` |

---

## 7. 未完了・保留・要データ（2026-09-16時点）

### ダッシュボード（③）
1. Vercel＋Neonへの実デプロイ（アカウント作成待ち。Vercel ProとNeon登録が必要）
2. CSV取込UI（マッピング→プレビュー→検証→確定、UPSERT・取込履歴・原本保持）
3. 売上・利益／広告分析／商品分析／在庫／目標／タスク／提案／レポート／マスターの各画面（現状プレースホルダ、ダッシュボードとデータ連携設定とユーザー管理のみ実装）
4. 提案ルールの閾値設定画面＋提案→タスク化
5. Playwright E2E
6. 楽天/Amazon/Meta/Googleの本番API連携・LLM提案は**追加開発範囲**（認証情報未提供）
7. 塚本さんの実メール未受領（仮ID `*@libetee.local`）

### AI日報システム（②）
- 9月の売上目標が未設定（`events-2026-09.json` は8月と同額1.1億を仮置き）。9月下旬の楽天イベントは未発表分を追記待ち
- `daily-plan-2026-09.json`（日別計画）が未作成→参謀レポートの計画比が9月は出ない
- `adcost-extra-2026-09.json`（手動広告費）が未作成
- `cost-map.json` の原価はスーツケースS/M/Lのみ。ファン・圧縮バッグ・アルミ・ドライヤー・洗顔ブラシ・アウトドアは原価待ち
- Amazon SP-API停止中の期間は `amazon-manual-*.json` で補完（キントーン添付があればそちら優先）
- 広告費: Amazon広告CSVが未添付の日は空欄（CPA Excelで明記）。キャンペーン接頭辞「F」の意味は未確認
- LINE通知は `APP_ENV=test` ではプレビューのみ（本番送信はトークン・宛先設定後）

### ドキュメントの欠落（既知）
- `daily-report-system/README.md` の構成図・コマンド表、`docs/phases.md` は「日報→Claude→LINE」時代のまま。売上自動取込・広告費・在庫・朝礼・CS・経費PDF・売上検算・アプリ統合は docs 化されていない（この統括室 §3・§4 が補完）
- Phase 8（本番1週間の安定稼働テスト）は手順提供のみで未完了扱い

### 財務・資金調達部
- 不足データ: 原価率（仕入原価）、販管費内訳（人件費・家賃・外注・物流費）、会計ソフトの試算表、借入明細、決算期
- 保有データ: 月次売上実績（7月¥103.4M・8月¥78.4M・9月は日次）、媒体別・商品別売上、広告費実績、在庫（個数・末端価格）、入荷予定

---

## 8. ドキュメント索引

| 知りたいこと | 読む場所 |
|---|---|
| 合言葉の正式手順・財務部の役割 | `CLAUDE.md` |
| システム全体・npmコマンド一覧 | `daily-report-system/README.md` |
| はじめての導入（非エンジニア向け） | `daily-report-system/docs/START-HERE.md`、`n8n-only-setup.md`、`setup-guide.md` |
| kintoneの実構造と読み取り方針 | `docs/actual-kintone-structure.md` |
| 各kintoneアプリの設計 | `docs/kintone-*.md`（intake / inventory / adcost / sales-detail / staff-report / ai-report） |
| Shopify / Amazon / 楽天 の自動取込 | `docs/shopify-自動連携.md`、`shopify-トークン発行.md`、`amazon-自動連携.md` |
| Chatwork / LINE の設定 | `docs/chatwork-setup.md`、`cs-chatwork-setup.md`、`line-setup.md`、`line-手順.md` |
| 自動送信（GitHub Actions）の考え方 | `docs/自動送信の設定.md` |
| レビュー返信下書きの運用 | `docs/review-reply-setup.md` |
| 売上レポート（CSV）の手順 | `docs/sales-report-setup.md` |
| バックアップ・安全性・エラー対応・テスト | `docs/backup-and-safety.md`、`error-handling.md`、`test-guide.md` |
| 実装フェーズ（1〜8） | `docs/phases.md` |
| CS専用ソフトを別プロジェクトで作る依頼文 | `docs/prompts/CS自動連携ソフト-依頼プロンプト.md` |
| 朝礼・広告費の設定ファイルの扱い | `config/chorei/README.md` |
| AI経営チーム（7部署）の設計 | `prompts/team/README.md` |
| ダッシュボード仕様・指標・CSV仕様・進捗・配置手順 | `dashboard/docs/spec.md`、`metrics.md`、`data-contracts.md`、`progress.md`、`ops.md` |
| 売上日報ツール（①）の使い方 | `README.md`（リポジトリ直下） |

---

## 9. この部屋の更新ルール（Claude向け）

1. 業務を1つ完了するたびに、この部屋の該当箇所（年表・カタログ・未完了）を更新してからコミットする。ダッシュボード作業は `dashboard/docs/progress.md` も併せて更新。
2. 新しい合言葉・部署・ワークフローを追加したら、`CLAUDE.md` とこの部屋の両方に載せる。
3. 数字（売上・広告費など）はこの部屋に書かない。ここは**所在と手順の索引**であり、数字は実行時に最新を取る。
4. 迷ったら `CLAUDE.md` → この部屋 → 各docs の順で正とする。
